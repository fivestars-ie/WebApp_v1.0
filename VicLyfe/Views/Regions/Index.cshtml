@model IEnumerable<VicLyfe.Models.Region>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Region_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Area_type)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Region_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Area_type)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Suburb_name }) |
            @Html.ActionLink("Details", "Details", new { id=item.Suburb_name }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Suburb_name })
        </td>
    </tr>
}

</table>
